<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 更新检测需要网络 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Xposed模块声明 -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.DeviceResetSpoofer">

        <!-- 标记这是一个Xposed模块 -->
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="@string/module_description" />
        <meta-data
            android:name="xposedminversion"
            android:value="82" />

        <!-- 内置下载 APK 安装：FileProvider 提供 content:// 访问 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="io.github.gjr787878.devicereset.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.ConfigActivity"
            android:exported="false"
            android:label="应用配置" />
        <activity
            android:name=".ui.AppPickerActivity"
            android:exported="false"
            android:label="选择应用" />
    </application>

</manifest>
