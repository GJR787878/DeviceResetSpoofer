<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- 标题和说明 -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="DeviceResetSpoofer"
            android:textSize="22sp"
            android:textStyle="bold"
            android:textColor="?attr/colorPrimary"
            android:layout_marginBottom="4dp"/>

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="清除应用数据后自动生成全新设备识别码"
            android:textSize="13sp"
            android:textColor="@android:color/darker_gray"
            android:layout_marginBottom="16dp"/>

        <!-- 已选数量 -->
        <TextView
            android:id="@+id/selected_count"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="已选择 0 个应用"
            android:textSize="14sp"
            android:textStyle="bold"
            android:layout_marginBottom="8dp"/>

        <!-- 搜索框 -->
        <EditText
            android:id="@+id/search_edit_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="搜索应用名称或包名..."
            android:inputType="text"
            android:padding="12dp"
            android:background="@android:drawable/edit_text"
            android:layout_marginBottom="8dp"/>

        <!-- 显示系统应用开关 -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="8dp">

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="显示系统应用"
                android:textSize="14sp"/>

            <androidx.appcompat.widget.SwitchCompat
                android:id="@+id/switch_show_system"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"/>
        </LinearLayout>

        <!-- 应用列表 -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recycler_view"
            android:layout_width="match_parent"
            android:layout_height="300dp"
            android:background="@android:color/white"
            android:layout_marginBottom="16dp"/>

        <!-- 分隔线 -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="@android:color/darker_gray"
            android:layout_marginBottom="16dp"/>

        <!-- Hook选项标题 -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="伪装选项"
            android:textSize="16sp"
            android:textStyle="bold"
            android:layout_marginBottom="12dp"/>

        <!-- 自动重置开关 -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="8dp">

            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical">

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="清除数据后自动换身份"
                    android:textSize="14sp"/>

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="核心功能，关闭后需手动重置"
                    android:textSize="11sp"
                    android:textColor="@android:color/darker_gray"/>
            </LinearLayout>

            <androidx.appcompat.widget.SwitchCompat
                android:id="@+id/switch_auto_reset"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"/>
        </LinearLayout>

        <!-- Android ID开关 -->
        <LinearLayout style="@style/SettingRow">
            <TextView style="@style/SettingText" android:text="伪装 Android ID"/>
            <androidx.appcompat.widget.SwitchCompat android:id="@+id/switch_hook_android_id"/>
        </LinearLayout>

        <!-- 广告ID开关 -->
        <LinearLayout style="@style/SettingRow">
            <TextView style="@style/SettingText" android:text="伪装 广告ID (AAID)"/>
            <androidx.appcompat.widget.SwitchCompat android:id="@+id/switch_hook_ad_id"/>
        </LinearLayout>

        <!-- IMEI开关 -->
        <LinearLayout style="@style/SettingRow">
            <TextView style="@style/SettingText" android:text="伪装 IMEI/MEID"/>
            <androidx.appcompat.widget.SwitchCompat android:id="@+id/switch_hook_imei"/>
        </LinearLayout>

        <!-- Build信息开关 -->
        <LinearLayout style="@style/SettingRow">
            <TextView style="@style/SettingText" android:text="伪装 设备型号 (Build)"/>
            <androidx.appcompat.widget.SwitchCompat android:id="@+id/switch_hook_build"/>
        </LinearLayout>

        <!-- MAC开关 -->
        <LinearLayout style="@style/SettingRow">
            <TextView style="@style/SettingText" android:text="伪装 MAC地址"/>
            <androidx.appcompat.widget.SwitchCompat android:id="@+id/switch_hook_mac"/>
        </LinearLayout>

        <!-- GSF开关 -->
        <LinearLayout style="@style/SettingRow">
            <TextView style="@style/SettingText" android:text="伪装 GSF ID"/>
            <androidx.appcompat.widget.SwitchCompat android:id="@+id/switch_hook_gsf"/>
        </LinearLayout>

        <!-- 运营商开关 -->
        <LinearLayout style="@style/SettingRow">
            <TextView style="@style/SettingText" android:text="伪装 运营商信息"/>
            <androidx.appcompat.widget.SwitchCompat android:id="@+id/switch_hook_carrier"/>
        </LinearLayout>

        <!-- 底部提示 -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="\n提示：在LSPosed中启用模块后，需勾选目标应用的作用域。" +
                "\n菜单中可手动重置某个应用的身份。"
            android:textSize="12sp"
            android:textColor="@android:color/darker_gray"
            android:layout_marginTop="16dp"/>

    </LinearLayout>
</ScrollView>
